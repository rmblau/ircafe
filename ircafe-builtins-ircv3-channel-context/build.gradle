apply from: rootProject.file('gradle/builtins-provider-conventions.gradle')

description = 'Built-in IRCafe IRCv3 channel context metadata and runtime provider.'

dependencies {
    implementation project(':ircafe-feature-ircv3-channel-context')
}
