/**
 * Filter matching, hidden-line placeholder rendering, and filtered history-batch flow helpers.
 *
 * <p>This package owns transcript behavior for hide/preview filters, including filtered-line run
 * tracking and the orchestration surface used by message, presence, spoiler, and system-line
 * rendering paths when a line is hidden by filter policy.
 */
package cafe.woden.ircclient.ui.chat.transcript.filter;
